const { createApp } = Vue;

createApp({
    data() {
        return {
            message: 'PHP ToDo List JSON'
        };
    },
    mounted() {
        // axios
        //     .get();
    }
}).mount('#app');